# v0.7.2 Evidence-aware Recommendation Design

## Goal

Improve Shopping Intelligence recommendation quality without weakening the v0.7.1 exact-price verification engine or Relay isolation guarantees.

The release focuses on four quality bottlenecks:
1. finalist selection before deep research,
2. review evidence trust/calibration,
3. value ranking under price uncertainty,
4. recommendation explanation and confidence semantics.

## Non-goals

- Do not change Provider v2 decisive-offer gates.
- Do not change Relay authentication, signing, or public/personalized price separation.
- Do not add an external LLM, vector database, or browser crawler runtime.
- Do not expand to new product categories in this release.
- Do not let search metadata become decisive product, review, or price evidence.

## Current bottlenecks

### Finalist selection

The current orchestrator shortlists candidates primarily by hard-constraint state and discoveryScore before deep research. This can over-favor products with stronger search exposure rather than products with stronger verified specifications and evidence diversity.

### Review evidence

Deep research currently converts search-result title/snippet text into qualitative evidence. A commerce-domain search hit may be treated as verified-purchase-like evidence even when the search result itself does not prove a verified purchase. Recency is not calibrated, and independence is primarily URL-path based.

### Value calculation

Value is currently cohort-relative price normalization with a neutral fallback for missing price. This can overstate value for candidates whose prices are not actually verified.

### Recommendation interpretation

recommendationScore and evidenceConfidence are already separate, which is correct, but the final result lacks an explicit recommendation tier that distinguishes strong recommendation from promising-but-under-evidenced candidates.

## Architecture

Keep the existing pipeline:

Query Planner -> Broad Discovery -> Normalization -> Light Enrichment -> Preliminary Selection -> Deep Research -> Exact Price Verification -> Final Ranking

Add three focused modules and extend one existing module:

1. `preliminary-ranking.ts`
   - selects deep-research finalists using evidence-aware preliminary quality rather than discoveryScore alone.
2. `review-trust.ts`
   - computes source trust, identity relevance, independence, sponsorship penalty, and recency factor.
3. `value-model.ts`
   - computes quality-adjusted value with explicit price uncertainty.
4. `ranking.ts`
   - consumes the above signals and emits recommendation tier and structured rationale.

Existing `review-intelligence.ts` remains responsible for topic/polarity extraction and topic-level consensus. Trust calibration is kept separate so topic parsing and source trust do not become tangled.

## 1. Evidence-aware preliminary selection

### Purpose

Choose the 8-10 shortlist and Top 5 deep-research finalists based on evidence quality, not search prominence.

### Inputs

For each candidate:
- hard-constraint state,
- discovery score,
- count and class of verified facts,
- official facts,
- source diversity,
- known model identity,
- preliminary price evidence availability,
- category-specific quality facts.

### Preliminary score

Use the following normalized components:
- hard-constraint state: 35%
- verified/official spec quality: 25%
- identity strength: 15%
- independent source diversity: 10%
- category-specific potential: 5%
- discovery score: 10%

Rules:
- EXCLUDED candidates never enter shortlist.
- ELIGIBLE outranks PRELIMINARY.
- discoveryScore contributes at most 10% of the preliminary score.
- verified page/official facts must outweigh search prominence.
- source diversity is capped after three independent source hosts.
- missing price does not exclude a product at this stage.
- ties are resolved by stronger verified-fact coverage, then stable candidate key.

The Top 5 deep-research finalists come from this score, not raw discovery order.

## 2. Review Trust Engine

### Review evidence contract

Extend `ReviewEvidence` with:
- `acquisitionMethod: AcquisitionMethod`
- `identityRelevance: number`
- `verifiedPurchaseConfidence: number`
- optional `authorKey`
- optional `claimFingerprint`

Search-result title/snippet evidence uses `acquisitionMethod='search_metadata'`.

A search result may still use `community_report`, `editorial_review`, or `sponsored_content` as its semantic source class, but search metadata alone MUST NOT be labeled `verified_purchase_review` and MUST have `verifiedPurchaseConfidence=0`.

`verified_purchase_review` requires direct evidence from a fetched page or trusted structured payload that explicitly identifies the review as a purchase-verified review. v0.7.2 does not fabricate this status from hostnames.

### Trust dimensions

Each review evidence item receives:
- `sourceTrust`
- `identityRelevance`
- `independenceConfidence`
- `recencyFactor`
- `sponsorshipFactor`
- `verifiedPurchaseConfidence`
- `effectiveWeight`

`effectiveWeight` is deterministic and bounded to 0..1.

### Source trust

Trust ordering:
1. directly verified purchase review evidence,
2. independent long-form user/community report,
3. editorial review,
4. ordinary search snippet describing a review,
5. sponsored content.

Default source-trust ceilings:
- directly verified purchase review: 1.00
- community report: 0.82
- editorial review: 0.78
- search-metadata-only review claim: 0.55
- sponsored content: 0.40

A Coupang/Naver Shopping URL alone is not sufficient to set verified purchase confidence above 0.

### Identity relevance

Review evidence must match the candidate identity.

Levels:
- exact model + variant/bundle: 1.0
- exact model, variant unclear: 0.8
- family/series only: 0.5
- generic category claim: 0.25
- conflicting model/variant: 0 and exclude from candidate consensus

Identity relevance is determined only from evidence content/structured identity, never from the search query that produced the result.

### Recency

Use a bounded decay based on `publishedAt` relative to the request `now` value:
- <=180 days: 1.00
- 181-365 days: 0.90
- >365 and <=730 days: 0.75
- >730 days: 0.60
- no trustworthy date: 0.70
- future/malformed date: treat as no trustworthy date (0.70), never throw

### Independence

Do not count evidence independently solely because the URL differs.

Deterministic claim normalization:
1. Unicode NFKC,
2. lower-case Latin text,
3. remove punctuation,
4. collapse whitespace,
5. normalize standalone prices/numbers to `#`,
6. truncate normalized claim to 240 characters.

`claimFingerprint` is the normalized claim string or a deterministic hash of it.

Independence family key:
- canonical registrable host,
- `authorKey` when available,
- `claimFingerprint`.

Rules:
- same host + same author + same fingerprint: collapse to one item.
- same host + missing author + same fingerprint: collapse to one item.
- same fingerprint syndicated across different hosts: first copy weight 1.0, second 0.35, later copies 0.15 each.
- distinct claims from genuinely independent hosts remain independent.

No fuzzy/embedding model is required for v0.7.2.

### Sponsorship

Sponsored evidence is not discarded, but:
- cannot alone produce review confidence above 0.60,
- cannot outweigh two or more independent non-sponsored user reports with comparable identity relevance,
- is explicitly surfaced in rationale when material.

## 3. Value model

### Principle

Value means quality-adjusted purchase merit, not cheapest price.

The module returns:
- `priceStatus: 'verified' | 'indicative' | 'unknown'`
- `priceConfidence`
- `priceBurden`
- `qualityAdjustedValue`
- `bestValueEligible`

### Price states

- `verified`: decisive public cash price available; confidence 0.95.
- `indicative`: exact-enough page price exists but ordinary decisive gates are incomplete; confidence 0.45.
- `unknown`: no trustworthy candidate price; confidence 0.

Rules:
- verified price can drive a best-value claim.
- indicative price may influence ordering but `bestValueEligible=false`.
- unknown price may not increase value and `bestValueEligible=false`.
- missing price must not receive the same neutral value as a verified mid-market price.
- small price differences cannot erase large quality differences.

### Calculation

1. Compute non-price product merit from the category dimensions.
2. Multiply merit by `0.75 + 0.25 * evidenceConfidence`; evidence quality moderates but does not erase product merit.
3. For verified/indicative cohort prices, calculate a bounded cohort price advantage between 0.60 and 1.00.
4. Combine:
   - verified: `0.72 * meritAdjusted + 0.28 * priceAdvantage`
   - indicative: `0.82 * meritAdjusted + 0.18 * priceAdvantage`
   - unknown: `0.90 * meritAdjusted`; no positive price bonus
5. Clamp 0..1.

The price engine remains unchanged. This module only consumes its output.

## 4. Final ranking and recommendation tier

Keep:
- `recommendationScore`
- `evidenceConfidence`

Add:
- `recommendationTier`
- `rationale`

Tiers are deterministic:

### STRONG_RECOMMENDATION

All must be true:
- recommendationScore >= 0.78
- evidenceConfidence >= 0.72
- candidate is ELIGIBLE
- no repeated negative topic with confidence >= 0.70 and at least two independent non-sponsored sources
- if the user expressed a value/price priority, `priceStatus='verified'`

### RECOMMENDED

All must be true:
- recommendationScore >= 0.68
- evidenceConfidence >= 0.55
- candidate is ELIGIBLE
- no material repeated-negative cap

### PROMISING_NEEDS_VERIFICATION

Use when candidate is ELIGIBLE and either:
- recommendationScore >= 0.68 but evidenceConfidence < 0.55, or
- recommendationScore >= 0.68 and a value-led query lacks verified price.

### CAUTION

Use when the candidate is otherwise rankable but:
- recommendationScore < 0.68, or
- a material repeated-negative topic exists, or
- evidence is internally conflicting enough to block a positive tier.

Rules:
- Tier never changes hard-constraint eligibility.
- A material repeated-negative topic caps the tier at CAUTION.
- Relay availability never changes public recommendation score, tier, value eligibility, or order.

## 5. Structured rationale

Replace raw internal dimension-name explanations with structured fields:
- `whyItRanks`
- `bestFor`
- `tradeoffs`
- `evidenceGaps`
- `repeatedNegativeTopics`
- `priceStatus`
- `bestValueEligible`

Rules:
- rationale is generated from structured scores/evidence, not free-form network content.
- every material rationale item must map to at least one provenance URL already present in candidate/review/offer evidence.
- unsupported superlatives such as "best value" are prohibited when `bestValueEligible=false`.

## 6. Category behavior

v0.7.2 improves only:
- `portable_display`
- `bedding`

No new category schemas are added.

Category-specific facts remain distinct.

Portable display priorities include:
- display quality,
- mobility/stand stability,
- smart features,
- build/durability,
- service/warranty,
- value.

Bedding priorities include:
- material/fill quality,
- tactile comfort,
- seasonal comfort,
- care,
- durability,
- allergy/dust behavior,
- value.

## 7. Error handling and partial results

- Candidate-local enrichment/review failures remain isolated.
- Deep research failure for one finalist does not discard others.
- Review trust calculation is deterministic and cannot throw for malformed dates/URLs.
- If review evidence is sparse, candidate remains rankable with lower evidenceConfidence and a visible evidence gap.
- If price is unavailable, candidate remains rankable for product merit but cannot be labeled best value.

## 8. Regression invariants

The following v0.7.1 invariants must remain true:
- wrong SKU never wins decisive price,
- wrong bundle never wins,
- unknown shipping never produces decisive cash,
- search metadata never wins decisive price,
- Relay personalized price never contaminates public ranking/history,
- Relay online/offline never changes public recommendation order,
- exact-product requests continue using the exact verification path,
- unsupported recommendation categories continue using the legacy path.

## 9. Golden quality benchmarks

Add deterministic fixtures/tests for:

1. **Quality beats modest price gap**
   - 359k mediocre TV vs 399k materially better TV.
   - Better TV should rank first when user values image quality/mobility.

2. **Search popularity does not dominate**
   - Candidate A has high discoveryScore but weak verified facts.
   - Candidate B has lower discoveryScore but strong official/page evidence.
   - B must enter Top 5 ahead of A.

3. **Sponsored volume does not dominate**
   - five sponsored positive posts vs two independent user reports.
   - sponsored volume must not create higher confidence/consensus.

4. **Duplicate complaint collapse**
   - repeated copied complaint URLs count as one independence family or receive the explicit syndicated diminishing weights.

5. **Recency matters**
   - older positives vs recent independent repeated defect reports.
   - recent evidence materially reduces quality confidence/rank.

6. **Ratings alone are insufficient**
   - 4.9 stars without qualitative evidence cannot create high quality score.

7. **Unknown price cannot win value label**
   - strong candidate with no verified/indicative price may rank on merit but `bestValueEligible=false`.

8. **Relay isolation**
   - same public evidence with Relay offline/online produces identical public order, score, tier, and bestValueEligible state.

9. **Bedding-specific quality**
   - stronger material/care/durability evidence can beat a cheaper weak-evidence alternative.

10. **Search snippet is not verified purchase**
   - a Coupang/Naver Shopping search hit without direct review proof has `verifiedPurchaseConfidence=0` and cannot become `verified_purchase_review` solely from hostname.

## 10. Release gates

v0.7.2 may merge only if:
- all existing v0.7.1 tests pass,
- all new quality golden tests pass,
- typecheck passes,
- production build passes,
- production dependency audit has 0 high/critical vulnerabilities,
- no external runtime dependency is added without a benchmark proving necessity,
- PR diff contains no generated artifacts or temporary workflows,
- main CI passes after merge,
- production smoke passes after deploy.

## 11. Expected file changes

New:
- `src/shopping/preliminary-ranking.ts`
- `src/shopping/review-trust.ts`
- `src/shopping/value-model.ts`
- corresponding tests/fixtures

Modified:
- `src/shopping/shopping-orchestrator.ts`
- `src/shopping/deep-research.ts`
- `src/shopping/review-intelligence.ts`
- `src/shopping/ranking.ts`
- `src/shopping/types.ts`
- `tests/shopping-*.test.ts`
- release metadata only at final release step

## 12. Complexity guardrails

To avoid quality degradation from architecture sprawl:
- each new module has one responsibility,
- ranking does not fetch network data,
- review trust does not parse product specs,
- preliminary ranking does not call price verification,
- value model does not alter Provider v2 offers,
- no circular imports among shopping modules,
- no single score is allowed to represent both merit and confidence,
- every new heuristic must have a deterministic regression test.
